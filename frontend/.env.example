# Copy to ".env". These are filled in after Phase 13 deployment - the
# deploy.js script also auto-writes ABI+address JSON files straight
# into src/contracts/, so this .env is mainly a human-readable record.
VITE_GANACHE_RPC_URL=http://127.0.0.1:7545
VITE_CHAIN_ID=1337
