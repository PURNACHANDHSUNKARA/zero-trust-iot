# Copy to ".env". Get this private key from Ganache's Quickstart
# workspace: click the little key icon next to any of the 10 accounts.
# These are FAKE test-network keys with fake ETH - still, never reuse
# a Ganache key on a real network, and never commit the real .env file.
GANACHE_RPC_URL=http://127.0.0.1:7545
DEPLOYER_PRIVATE_KEY=0xreplace_with_a_ganache_account_private_key
