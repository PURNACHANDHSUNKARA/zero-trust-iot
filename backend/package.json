{
  "name": "zero-trust-iot-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-Trust IoT Security Framework Backend Engine & API Server",
  "scripts": {
    "start": "gunicorn --bind 0.0.0.0:${PORT:-8000} --chdir python-security server:app",
    "dev": "python run_all.py",
    "backend:run": "gunicorn --bind 0.0.0.0:${PORT:-8000} --chdir python-security server:app",
    "install:all": "npm --prefix blockchain install",
    "blockchain:test": "npm --prefix blockchain test",
    "blockchain:deploy": "npm --prefix blockchain run deploy"
  }
}
